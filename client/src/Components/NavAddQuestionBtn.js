import React from "react";

const NavAddQuestionBtn = () => {
  return (
    <div className="add-question-btn">
      <button>Add Question</button>
    </div>
  );
};

export default NavAddQuestionBtn;
